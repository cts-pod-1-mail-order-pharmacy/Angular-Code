import { RefillStatusClass } from './refill-status-class';

describe('RefillStatusClass', () => {
  it('should create an instance', () => {
    expect(new RefillStatusClass()).toBeTruthy();
  });
});
